{
  "name": "@modelroom/core",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "ModelRoom Orchestrator core. Zero runtime dependencies by design: the npm registry is unreachable in this sandbox, and the risk-bearing logic (cost ledger, idempotency, state guards, artifact leases) needs no framework.",
  "engines": {
    "node": ">=22.18"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json",
    "test": "node --test src/test/*.test.ts",
    "verify": "npm run typecheck && npm run test"
  },
  "dependencies": {},
  "devDependencies": {}
}
